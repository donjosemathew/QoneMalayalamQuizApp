import { StyleSheet } from "react-native";
import { Dimensions } from "react-native";
const fontStyles = StyleSheet.create({
  question: {
    textAlign: "left",
    fontSize: 26,
    width: "100%",
    fontWeight: "500",
    color: "#fff",
    fontFamily: "MalayalamFontBold",
    lineHeight: 35,
    letterSpacing: 1,
  },
});
export default fontStyles;
